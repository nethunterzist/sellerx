#!/bin/sh
# Maven Wrapper script (regenerated)

set -e

MAVEN_PROJECTBASEDIR="$(cd "$(dirname "$0")" && pwd -P)"
export MAVEN_PROJECTBASEDIR

MAVEN_WRAPPERJAR="$MAVEN_PROJECTBASEDIR/.mvn/wrapper/maven-wrapper.jar"

if [ ! -f "$MAVEN_WRAPPERJAR" ]; then
    echo "Maven wrapper jar not found at $MAVEN_WRAPPERJAR"
    exit 1
fi

exec java -Dmaven.multiModuleProjectDirectory="$MAVEN_PROJECTBASEDIR" -cp "$MAVEN_WRAPPERJAR" org.apache.maven.wrapper.MavenWrapperMain "$@"
